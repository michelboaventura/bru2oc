meta {
  name: get-user
  type: http
  seq: 3
}

get {
  url: https://api.example.com/users/:id
}

params:path {
  id: {{user_id}}
}

auth:bearer {
  token: {{access_token}}
}

assert {
  res.status: eq 200
}
